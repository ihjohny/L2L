# Claude Code Instructions

## Commit Policy

**Do not commit changes without an explicit commit prompt from the user.**

Always ask for permission before creating any git commits. Wait for the user to explicitly request a commit (e.g., "commit the changes", "make a commit", etc.).
